<%= form_with(model: dispensary, local: true) do |form| %>
  <% if dispensary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dispensary.errors.count, "error") %> prohibited this dispensary from being saved:</h2>

      <ul>
      <% dispensary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dispensary_name %>
  </div>

  <div class="field">
    <%= form.label :full_address %>
    <%= form.text_field :full_address, id: :dispensary_full_address %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :dispensary_email %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number, id: :dispensary_phone_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
